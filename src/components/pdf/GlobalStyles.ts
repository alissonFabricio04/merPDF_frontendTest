import { createGlobalStyle } from "styled-components"

export const GlobalStyle = createGlobalStyle`
    body {
        padding: 0.30rem;
        padding-bottom: 0rem;
        background: linear-gradient(50deg,#ffcd1e .48%,#ff6600);
        width: 100%;
    }
`
