import styled from "styled-components";

export const Container = styled.div`

    img.img-logo {
        width: 200px;
    }

    h1 {
        font-size: 1rem;
        font-weight: 900;
    }

    p {
        font-size: 1rem;
        font-weight: 700;
    }

    p.last {
        padding-bottom: 2rem;
    }
`