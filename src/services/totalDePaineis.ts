export function oi() {

}

/*import { VarTotalPaineis } from "../App"

import { potenciaDeGeracaoDeModulos } from "./potenciaModulos";
import { mediaConsumo } from "./mediaConsumo"

export function totalDePaineis() {
    const VarPotenciaTotalModulos = potenciaDeGeracaoDeModulos()
    const VarConsumo = mediaConsumo()
    const totalDeModulosCalculados = (VarConsumo * VarPotenciaTotalModulos) * 1.2
    const totalDeModulosAplicados = Math.ceil(totalDeModulosCalculados)

    const potenciaSistema = (VarTotalPaineis[0] * totalDeModulosAplicados) / 1000
    const potenciaInversor = potenciaSistema / 1.2


    return {
        totalDeModulosAplicados,
        potenciaSistema,
        potenciaInversor
    }
}
*/